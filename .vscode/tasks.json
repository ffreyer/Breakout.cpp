{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Main",
            "windows": {
                // "command": "${workspaceFolder}/manual_build.bat",
                "command": "${workspaceFolder}/build_all.bat",
            },
            "linux": {
                "command": "${workspaceFolder}/build.sh",
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}